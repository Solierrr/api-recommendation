name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --only-binary=:all: pip==26.2.1
          if [ -f requirements-dev.txt ]; then
            pip install --only-binary=:all: -r requirements.txt -r requirements-dev.txt
          else
            pip install --only-binary=:all: -r requirements.txt
          fi

      - name: Run tests with coverage
        run: |
          set +e
          pytest --cov=app --cov-report=xml --cov-report=term-missing --junitxml=test-results.xml
          code=$?
          set -e
          if [ "$code" -eq 5 ]; then
            echo "Nenhum teste coletado ainda nesta branch — tratando como sucesso."
            exit 0
          fi
          exit "$code"

      - name: Upload coverage
        if: hashFiles('coverage.xml') != ''
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: |
            coverage.xml
            test-results.xml
